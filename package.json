{
  "name": "garuchan-res-checker",
  "description": "manifest.json description",
  "private": true,
  "version": "1.3.0",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build --mode development --mv3",
    "zip": "node scripts/zip-release.mjs",
    "compile": "tsc --noEmit",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "@sentry/vite-plugin": "^5.1.1",
    "typescript": "^5.9.3",
    "wxt": "^0.20.6"
  },
  "dependencies": {
    "@sentry/browser": "^10.42.0",
    "node-html-parser": "^7.0.2"
  }
}
